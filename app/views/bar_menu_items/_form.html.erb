<%= form_for(@bar_menu_item) do |f| %>
  <% if @bar_menu_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bar_menu_item.errors.count, "error") %> prohibited this bar_menu_item from being saved:</h2>

      <ul>
      <% @bar_menu_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :price %><br>
    <%= f.number_field :price %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
