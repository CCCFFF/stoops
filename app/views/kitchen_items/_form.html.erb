<%= form_for(@kitchen_item) do |f| %>
  <% if @kitchen_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@kitchen_item.errors.count, "error") %> prohibited this kitchen_item from being saved:</h2>

      <ul>
      <% @kitchen_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :kitchen_menu_item_id %><br>
    <%= f.number_field :kitchen_menu_item_id %>
  </div>
  <div class="field">
    <%= f.label :ticket_id %><br>
    <%= f.number_field :ticket_id %>
  </div>
  <div class="field">
    <%= f.label :kitchen_closeout %><br>
    <%= f.check_box :kitchen_closeout %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
